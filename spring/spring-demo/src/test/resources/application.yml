spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:5.7.34://localhost/wethura?TC_INITSCRIPT=META-INF/initial.sql
    username: root
    password: root

logging.level:
  root: DEBUG