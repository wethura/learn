mod threads;
mod thread_local_variable;
mod thread_logic_control;
mod thread_message_passing;
mod thread_async;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
